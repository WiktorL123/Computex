import Filter from "@/app/components/Filter";

export default function CategoryLayout({children}){
    return (
        <div>
            <main>
                {children}
            </main>
        </div>
    )
}